<mapper namespace="com.su.DAO.UserDAO" xmlns="http://mybatis.org/schema/mybatis-mapper">
    <select id="login" resultType="User">
        select * from t_user where username = #{paraml} and password = #{param2}
    </select>
</mapper>