<mapper namespace="com.su.DAO.AdminDAO" xmlns="http://mybatis.org/schema/mybatis-mapper">
    <select id="login" resultType="Admin">
        select * from t_admin where username = #{paraml} and password = #{param2}
    </select>
</mapper>